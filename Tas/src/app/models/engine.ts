export class Engine {
  fuel : string;
  power: number;
  drive: string;

  constructor(
    fuel : string,
  power: number,
  drive: string,

  ) {
    this.fuel = fuel;
    this.power = power;
    this.drive = drive;
  }

}
